# Copiá este archivo a .env.local y completá con los datos de tu proyecto Supabase.
# .env.local está gitignoreado (los secretos NO se versionan).
# Solo se usa la anon key (pública, protegida por RLS). La service_role key NUNCA
# va en el frontend.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
